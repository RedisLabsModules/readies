
ifneq ($(OS),macos)
	LIBOMP=-fopenmp
else
	LIBOMP_PREFIX=$(call cached-shell,LIBOMP_PREFIX,brew --prefix libomp)
	ifeq ($(STATIC_OMP),1)
		LIBOMP=$(LIBOMP_PREFIX)/lib/libomp.a
	else
		LIBOMP=-lomp -L$(LIBOMP_PREFIX)/lib -Wl,-no_compact_unwind
	endif
endif

export LIBOMP
